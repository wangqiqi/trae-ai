# Trae AI 超级团队依赖
# 此项目使用 Python 3.7+ 以上版本
# 核心依赖均为 Python 标准库，无需额外安装
# 如需要特定功能可以添加额外依赖

# 可选依赖 (根据需要取消注释
# requests>=2.31.0       # HTTP请求（如需网络功能
# jinja2>=3.1.2         # 模板渲染
# click>=8.1.0           # 命令行工具
# rich>=13.4.0          # 富文本输出